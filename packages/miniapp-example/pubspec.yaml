name: miniapp_example
description: An example mini-app demonstrating PaymentAPI usage
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_eval: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

analyzer:
  enable-experiment:
    - dot-shorthands
