targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
      drift_dev:
        options:
          databases:
            app_database: lib/database/drift_db.dart
